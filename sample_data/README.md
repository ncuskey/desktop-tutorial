This directory is used by the Forex Strategy Research Lab prototype.

The runner script generates deterministic mock CSV data here on demand:

- `forex_mock_h1.csv`

The CSV is ignored by Git so repeated research runs do not create noisy diffs.
