Sample FX OHLCV data is generated on first prototype run and written to `sample_data/fx_hourly_sample.csv`.

The generated dataset includes hourly bars for:

- EURUSD
- GBPUSD
- USDJPY
- AUDUSD

Timeframes `H4` and `D1` are derived through resampling inside the research lab.
